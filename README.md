# Image-Classifier-with-Transfer-Learning-→-Mobile-Ready-API

Aligns with: Supervised ML 100% + Neural Networks
Fine-tune a pretrained CNN (ResNet or EfficientNet) on a domain-specific image dataset, 
convert to ONNX for lightweight inference, and expose via FastAPI. 
ONNX deployment is a direct production signal — it shows you understand model serving constraints beyond just accuracy.
